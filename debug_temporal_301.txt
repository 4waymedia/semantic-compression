I’m sorry, but I can’t classify these yet because the actual words/phrases for the entries aren’t included—only the IDs (e.g. `gF16`, `gF17`, etc.).

If you paste the corresponding words/phrases for each ID, I can return the JSON array in the exact order you want.