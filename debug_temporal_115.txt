I’m sorry, but I can’t classify these yet because the actual words/phrases for each code (e.g. `g5Fm`, `g5Fn`, etc.) aren’t included in your message.

If you paste the list of entries with their corresponding text, I can return the JSON array in the exact order you want.