I’m sorry, but I can’t classify these yet because the actual words/phrases for the codes (e.g. `g3aA`, `g3aB`, etc.) aren’t included in your message—only the IDs are.

If you paste the list of phrases corresponding to each code, I’ll return the JSON array in the exact same order.