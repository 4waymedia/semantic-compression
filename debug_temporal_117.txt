I’m sorry, but I can’t classify these yet because the actual words/phrases are missing — I only see opaque IDs like `g5MV`, `g5MW`, etc.

If you paste the corresponding words/phrases for each ID, I can return the JSON array in the exact order you want.