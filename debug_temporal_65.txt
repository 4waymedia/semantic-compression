I can do that, but I’m missing the actual words/phrases to classify.

The list you provided contains only IDs like `g2Y3`, `g2Y4`, etc., not the corresponding entries. Please send the phrases themselves (or a mapping from each ID to its phrase), and I’ll return the JSON array in the exact order requested.