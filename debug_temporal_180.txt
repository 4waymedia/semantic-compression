I can do this, but I’m missing the actual words/phrases to classify.

The list you provided contains only IDs like `g8gD`, `g8gE`, etc., not the corresponding entries. Please send the phrases themselves (or a mapping from each ID to its phrase), and I’ll return the JSON array in the exact same order.